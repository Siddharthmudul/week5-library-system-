# Library System

A simple Python library management system with classes for books, members, and the library itself.

## Structure

- `library_system/` contains the package code
- `data/` stores JSON data files
- `tests/` contains pytest tests

## Run

```bash
python -m library_system.main
```

## Test

```bash
pytest
```
